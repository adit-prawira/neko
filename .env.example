# Neko — environment variable examples
#
# Copy this file to `.env` and uncomment the variables you want to override.
# All variables are optional; neko ships with sensible defaults.

# Override the data directory. Defaults to $HOME/.neko when unset.
# The directory is created automatically on first neko invocation.
# NEKO_HOME=/var/neko
