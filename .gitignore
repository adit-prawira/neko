# — Build output —
/neko
engine/target/
*.o
*.a
*.dylib
*.so
simd/test_*

# — Embedded model —
engine/models/*.onnx
engine/models/*.onnx.gz

# — Go —
vendor/

# — Proto codegen (regenerated by `make proto`) —
internal/gen/**/*.pb.go
internal/gen/**/*_grpc.pb.go
# keep the directory itself tracked
!internal/gen/.gitkeep

# — Docs (ignore all .md except core reference docs) —
*.md
!README.md
!docs/BACKGROUND.md
!docs/ARCHITECTURE.md
!docs/API.md

# — IDE / editor —
.vscode/
.idea/
*.swp
*.swo

# — OS —
.DS_Store
Thumbs.db
